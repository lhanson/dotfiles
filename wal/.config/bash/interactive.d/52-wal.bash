################################################################################
# wal.bash - settings for wal
################################################################################

# Import colorscheme from 'wal' asynchronously
(cat $XDG_CACHE_HOME/wal/sequences &)
