################################################################################
# Compton config
# Good example: https://github.com/Simply-Sublime/rice/blob/master/config/.config/compton/compton.conf
################################################################################

backend = "glx";
fading = true;				# Fade windows during opacity changes.
fade-delta = 3;				# The time between steps in a fade in milliseconds. (default 10).
inactive-dim = 0.1;			# Dim inactive windows. (0.0 - 1.0, defaults to 0).
mark-wmwin-focused = true;	# Try to detect WM windows and mark them as active.

#### Shadows
shadow = true;				# Enabled client-side shadows on windows.
clear-shadow = true;		# Zero the part of the shadow's mask behind the window (experimental).
# Hard-code shadow RGB for now, but ideally this would be
# read from the current wal theme.
shadow-red = 0.87;
shadow-green = 0.27;
shadow-blue = 0.56;
shadow-radius = 17;
shadow-exclude = ["!focused"];
