# Files which would conflict with stow are copied here
backup
# Used by setup.sh to bootstrap stow itself
stow
# Too big at the moment, not sure if I want to haul them around
wallpapers
# Ignore symlink kludge to let weechat find the CA certs on OS X and Linux
weechat/.config/weechat/ca-certs
